# AWS Terraform Security Baseline ([DEMO])

Illustrates: KMS rotation, S3 encryption + public access block + versioning, VPC flow logs, lean IAM policy document.

```bash
# dry planning only — requires AWS creds you own
terraform init
terraform plan
```
